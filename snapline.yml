version: 1
stack:
  framework: next
# Semantic CSS classes and custom properties live in app/globals.css.
rules:
  noRawHex: error
  noInlineStyle: warn
  noArbitraryTailwind: warn
  noRawPaletteColor: warn
  noDuplicateComponents: warn
fix:
  safeAutofix: false
  preferAgentRepair: true
